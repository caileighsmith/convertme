[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "convertme-api"
version = "0.1.0"
description = "ConvertMe backend API"
requires-python = ">=3.12"
